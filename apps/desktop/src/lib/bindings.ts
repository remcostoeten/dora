// Compatibility re-export. The generated Tauri contract has one canonical
// location so the desktop shell and Studio cannot drift independently.
export * from '../../../../packages/studio/src/lib/bindings'
