    {
  "name": "dora",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:desktop": "bun --cwd apps/desktop run test",
    "test:rust": "bun --cwd apps/desktop run test:rust",
    "desktop:run": "bun --cwd apps/desktop run dev",
    "desktop:dev": "bun run --cwd apps/desktop tauri dev",
    "desktop:build": "bun run --cwd apps/desktop tauri build",
    "release:gen": "bun tools/scripts/generate-release.ts",
    "ai:setup": "bun tools/scripts/setup-local-ai.ts",
    "setup:ai": "bun tools/scripts/setup-local-ai.ts",
    "runner": "./dora-runner"
  },
  "devDependencies": {
    "@types/sqlite3": "^5.1.0",
    "turbo": "^2.3.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "zustand": "^5.0.10",
    "sqlite3": "^5.1.7"
  }
}