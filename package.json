{
  "name": "dora",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "desktop:run": "bun run --cwd apps/desktop dev",
    "desktop:dev": "bun run --cwd apps/desktop tauri dev",
    "desktop:build": "bun run --cwd apps/desktop tauri build",
    "docs:dev": "bun run --cwd apps/api-docs dev",
    "docs:build": "bun run --cwd apps/api-docs build",
    "release:gen": "bun tools/scripts/generate-release.ts",
    "ai:setup": "bun tools/scripts/setup-local-ai.ts",
    "setup:ai": "bun tools/scripts/setup-local-ai.ts",
    "runner": "./dora-runner"
  },
  "devDependencies": {
    "@types/sqlite3": "^5.1.0",
    "turbo": "^2.3.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "zustand": "^5.0.10",
    "sqlite3": "^5.1.7"
  }
}